---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] X-004: 동시접속자 상한 체크 (완전 무료 운영) [Query]"
labels: 'feature, infra, priority:medium'
assignees: ''
---

## 🎯 Summary
- 기능명: [X-004] 동시접속자 상한 체크(REQ-NF-011, 신규)
- Read/Write 구분: **Query**(COUNT 조회, DB 쓰기 없음)
- 목적: "완전 무료" 인프라 운영 전제(C-TEC-008)를 실제로 지키기 위해, 동시 활성 공유 객체 수를 조회해 초대 발급 여부를 판단할 근거를 제공한다. 실제 저하 처리(초대 거부)는 S-002가 이 결과를 사용해 수행한다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- **⚠️ 이 태스크의 근거는 아직 `SRS_V0_9.md` 본문에 병합되지 않았다.** 설계 원문은 `SRS_V0_9-AI-작업지시서.md` TASK-B1(C-TEC-008 신설)·TASK-B4(§11.3 신설, REQ-NF-011)·TASK-B5(pgbouncer 풀링)를 그대로 따른다. 착수 전 이 세 TASK 항목이 실제로 SRS에 반영됐는지 먼저 확인할 것.
- 3관점 검토: `SRS_V0_9-작업지시서-변경사항-3관점-검토.md` §2.3 — 이 기능이 "기술스택 정합화"가 아니라 작은 신규 기능이라는 지적을 반드시 먼저 읽을 것
- 인수 조건: companion **TEST-X-004** 참조(원 SRS §9의 AC가 아니라 이 프로젝트가 직접 정의한 시나리오임에 유의)
- 구 버전: `SRS_project/tasks/TASK-015-NF011-동시접속상한.md`(v1)에서 Query 부분만 분리(v1에는 저하 로직도 함께 있었으나, v2에서는 S-002가 이 결과를 소비하는 구조로 재배치)

## ✅ Task Breakdown (실행 계획)
- [ ] (선행) `SRS_V0_9-AI-작업지시서.md` TASK-B1·B4·B5가 SRS 본문에 반영됐는지 확인, 미반영이면 먼저 반영
- [ ] `SharedSpace` 테이블에 대한 `COUNT` 쿼리로 현재 활성 공유 객체 수를 조회하는 `checkCapacityBeforeInvite(): Promise<{ available: boolean }>` 작성(순수 조회 함수, 새 인프라·큐 도입 없음)
- [ ] 배포 환경 `DATABASE_URL`을 Supabase pgbouncer 풀링 연결로 전환(TASK-B5, 포트 6543) — C-000에서 이미 준비된 설정 확인

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-X-004** 참조. 이 시나리오는 원 SRS AC가 아니라 v1 `TASK-015`에서 이 프로젝트가 직접 정의한 것이다.

## ⚙️ Technical & Non-Functional Constraints
- **이 태스크는 원 SRS(`같이보기-srs-v1_0.md`)의 REQ-FUNC-001~025 어디에도 명시되지 않은 신규 요구사항이다.** 착수 전 REQ-NF-011을 정식 NFR로 등록할 것을 권고
- 상한 값 "N"은 `[TBD]` — 배포 직전 Vercel/Supabase 무료 티어 한도 재확인 후 확정
- 순수 조회 함수만 허용 — 실제 초대 거부 로직(저하 처리)은 S-002에서 이 결과를 소비하는 형태로 구현(Query/Command 분리)

## 🏁 Definition of Done (DoD)
- [ ] TEST-X-004의 모든 테스트가 통과하는가?
- [ ] REQ-NF-011이 SRS 문서에 정식 등록되었는가(누락 시 이 태스크를 완료로 처리하지 않는다)?
- [ ] `DATABASE_URL`이 pgbouncer 풀링 연결로 확인되었는가?

## 🚧 Dependencies & Blockers
- Depends on: C-000(pgbouncer 설정), S-001(SharedSpace 카운트 대상)
- Blocks: S-002(초대 발급 시 이 함수의 결과로 저하 처리 여부를 판단)
