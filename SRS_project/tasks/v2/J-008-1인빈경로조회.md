---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] J-008: 1인 빈 경로 조회 [Query]"
labels: 'feature, backend, priority:high'
assignees: ''
---

## 🎯 Summary
- 기능명: [J-008] 1인 빈 경로 조회 — REQ-FUNC-009
- Read/Write 구분: **Query**(조회 전용, 상태 변경 없음)
- 목적: B가 참여하지 않았거나 조건을 입력하지 않은 상태에서도 A가 자신의 조건만으로 실부담·판정을 볼 수 있게 한다. 시드 데이터로 판정 엔진을 화면까지 가장 빨리 검증하는 종단 경로.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-009, §9.2 AC-09-01·02
- 데이터 계약: C-001의 `JudgmentResultSet` 타입
- 인수 조건: companion **TEST-J-008** 참조
- 구 버전: `SRS_project/tasks/TASK-003-FR009-1인빈경로.md`(v1, 동일 범위 — 이미 순수 Query였음)

## ✅ Task Breakdown (실행 계획)
- [ ] 테스트용 시드 스크립트 작성 — `SharedSpace` 1건 + A `Person` 1건(B 없음)을 Prisma seed로 직접 생성
- [ ] `queries/judgment.queries.ts`에 `getSoloJudgment(personId): Promise<JudgmentResultSet>` 작성 — B 부재를 진입 차단 조건으로 사용하지 않음
- [ ] `app/spaces/[spaceId]/judgments/page.tsx`(Server Component)에서 B 미참여 여부와 무관하게 A 조건만 반영된 결과를 렌더링
- [ ] "출근 안 함" 선택 시(J-006의 NOT_APPLICABLE 결과) 통근·교통비 행을 렌더링에서 제외

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-J-008** 참조. 관련 AC: AC-09-01(정상), AC-09-02(예외 — 1인분 전제 미표시).

## ⚙️ Technical & Non-Functional Constraints
- 이 태스크는 순수 조회다 — DB에 아무것도 쓰지 않는다(판정 결과 저장은 J-001가 이미 수행한 것을 읽기만 함)
- REQ-FUNC-021(전제 공개)은 X-001에서 통합 예정 — 이 태스크에서는 "1인 입력만 반영" 문구만 정적으로 표시

## 🏁 Definition of Done (DoD)
- [ ] TEST-J-008의 모든 테스트가 통과하는가?
- [ ] 시드 스크립트가 README에 문서화되었는가?

## 🚧 Dependencies & Blockers
- Depends on: J-001(저장된 판정 결과를 읽음), C-000
- Blocks: 없음(검증용 태스크, 다른 태스크의 선행 조건 아님)
