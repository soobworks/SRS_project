---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] V-002: 균형 제시 가드레일 — 균형 비교 렌더링 [Query/UI]"
labels: 'feature, frontend, priority:high'
assignees: ''
---

## 🎯 Summary
- 기능명: [V-002] 균형 제시 가드레일 — REQ-FUNC-025
- Read/Write 구분: **Query/UI**(조회된 데이터를 렌더링만 함, DB 쓰기 없음)
- 목적: 판정·trade-off·최종 비교 화면에서 A/B를 동일 비중으로 표시하고 총점·추천 배지를 원천 차단하는 공용 컴포넌트를 만든다. V-001(매칭 로직)과 분리해 UI 컴포넌트만 독립적으로 재사용 가능하게 한다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-025, §9.6 AC-25-01~03
- 컴포넌트 설계: `SRS_V0_9.md` §2.2 — `components/domain/balanced-comparison.tsx`
- 인수 조건: companion **TEST-V-002** 참조
- 구 버전: `SRS_project/tasks/TASK-009-FR017025-방문후보결정.md`(v1) 후반부에서 분리

## ✅ Task Breakdown (실행 계획)
- [ ] `components/domain/balanced-comparison.tsx` 작성 — A/B 조건을 동일 순서·시각적 비중으로 표시하는 공용 레이아웃 컴포넌트
- [ ] 총점·추천 배지·복합 순위를 렌더링하는 prop/slot 자체를 컴포넌트 API에서 제공하지 않음(구조적으로 불가능하게 설계)
- [ ] 색상은 충족·미충족·확인 상태에만 사용, A/B 구분은 위치·라벨로만(지도 경로선 예외 처리)

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-V-002** 참조. 관련 AC: AC-25-01(정상), AC-25-02(실패/경계 — 제품 정체성 핵심), AC-25-03(경계).

## ⚙️ Technical & Non-Functional Constraints
- **제품 정체성 제약(협상 불가):** 이 컴포넌트는 판정 화면(J-006 소비처), trade-off 화면(R-001), 방문 후보 화면(V-001) 전부에서 공용으로 재사용되어야 총점 금지 원칙이 화면 전체에 일관되게 적용된다
- 이 태스크는 DB에 아무것도 쓰지 않는다 — 순수 프레젠테이션 컴포넌트

## 🏁 Definition of Done (DoD)
- [ ] TEST-V-002의 모든 테스트가 통과하는가?
- [ ] 코드 리뷰에서 총점·추천배지 prop 자체가 컴포넌트 API에 존재하지 않음을 확인했는가?

## 🚧 Dependencies & Blockers
- Depends on: C-001
- Blocks: R-001(양보 문장 화면이 이 컴포넌트 재사용), X-001(전제 공개와 함께 조합)
