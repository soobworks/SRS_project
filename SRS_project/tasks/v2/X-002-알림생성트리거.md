---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] X-002: 알림 생성·조회 (Notification Command + Query) [Command+Query]"
labels: 'feature, backend, priority:medium'
assignees: ''
---

## 🎯 Summary
- 기능명: [X-002] 선택·조건 변경 알림 — 생성(REQ-FUNC-024) + 조회(REQ-FUNC-024)
- Read/Write 구분: **Command+Query** — 생성(Notification 레코드 쓰기)과 조회(폴링형 읽기)를 한 파일에 두되, 함수는 명확히 분리된 두 모듈로 구현한다(`condition-change-notifier.ts` vs `notification.queries.ts`).
- 목적: 조건 입력·변경, 완화 제안, 후보 변경·소진, 방문 후보 상태 변경 시 상대에게 알림을 생성하고, 상대가 재방문 시 이를 확인할 수 있게 조회를 제공한다.
- 병합 근거(`TASK-개수-축약-분석.md` #6): `Notification`이라는 같은 작은 엔티티에 대한 생성/조회이며, 원 SRS에서도 REQ-FUNC-024 하나로 서술됐고 애초부터 테스트 companion(TEST-X-NOTIF)을 공유하고 있었다. CQRS 분리 원칙은 함수/파일 단위로는 그대로 유지한다(생성 로직과 조회 로직을 절대 한 함수에 섞지 않음).

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-024, §9.6 AC-24-01·02
- 데이터 모델: `SRS_V0_9.md` §6.2 `Notification`, enum `NotificationType`
- 조회 위치: `SRS_V0_9.md` §2.2 — `queries/notification.queries.ts`(전량 Query)
- 인수 조건: companion **TEST-X-NOTIF**(X-002 생성·조회 통합) 참조
- 구 버전: `SRS_project/tasks/TASK-017-FR024-알림.md`(v1)에서 분리됐던 v2 X-002/X-003을 다시 한 파일로 통합(2026-08-26, 태스크 개수 축약)

## ✅ Task Breakdown (실행 계획)

**생성(Command):**
- [ ] `domain/notification/condition-change-notifier.ts` 작성 — 4개 트리거(조건 변경, 완화 제안 발송, 후보 변경·소진, 방문 후보 상태 변경)를 이벤트 타입으로 정의
- [ ] I-001(조건 입력), R-003(완화 제안), S-001/V-003(후보 변경·소진), V-001(방문 후보 결정) 각 Command 내부에 이 알림 생성 함수를 호출하는 연동 지점 삽입(공통 헬퍼 1줄 호출로 최소 침습)

**조회(Query):**
- [ ] `queries/notification.queries.ts`에 `getUnreadNotifications(personId)` 조회 함수 작성
- [ ] 인앱(앱 내) 알림 표시 UI만 우선 구현, 푸시·문자 채널은 `[TBD]`

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-X-NOTIF** 참조. 관련 AC: AC-24-01(정상), AC-24-02(예외).

## ⚙️ Technical & Non-Functional Constraints
- 알림 채널(푸시·문자·앱 내)은 정책 미확정이므로 `[TBD]`로 두고, 이 태스크는 `Notification` 레코드 생성·조회까지만 다룬다(발송 채널은 범위 밖)
- 읽음 처리 등 조회 이외의 상태 변경이 필요하면 별도 Command로 추가 분리할 것(이 태스크의 조회 함수 자체는 DB에 쓰지 않는다)
- 생성 로직과 조회 로직은 반드시 서로 다른 파일/함수로 유지 — 병합은 "티켓" 단위이지 "함수" 단위가 아니다

## 🏁 Definition of Done (DoD)
- [ ] TEST-X-NOTIF의 생성(Command)·조회(Query) 테스트가 모두 통과하는가?
- [ ] 4개 트리거 지점 전부에서 알림이 실제로 생성되는지 통합 테스트로 확인했는가?

## 🚧 Dependencies & Blockers
- Depends on: I-001, R-003, V-001, V-003(후킹 대상 Command들이 선행 완료돼야 함)
- Blocks: 없음
