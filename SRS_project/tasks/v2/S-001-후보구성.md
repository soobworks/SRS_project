---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] S-001: 관심매물에서 비교 후보 구성 [Command]"
labels: 'feature, backend, priority:high'
assignees: ''
---

## 🎯 Summary
- 기능명: [S-001] 관심매물에서 비교 후보 구성 — REQ-FUNC-001
- Read/Write 구분: **Command**(DB 쓰기)
- 목적: A가 관심매물 중 1~5개를 선택해 공유 객체(SharedSpace) 초안을 구성한다. 판정 엔진이 검증된 뒤 "진짜 데이터"를 흘려보내는 실사용자 흐름의 입구.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-001, §9.1 AC-01-01·02
- Server Action: `SRS_V0_9.md` §3.1 — `createSharedSpaceDraft(listingIds: string[]): Promise<SharedSpace>`(C-001 타입)
- 외부 연동: `SRS_V0_9.md` §3.3, C-002(목업 픽스처)를 통해 접근
- 인수 조건: companion **TEST-S-001** 참조
- 구 버전: `SRS_project/tasks/TASK-007-FR001-후보구성.md`(v1, 동일 범위)

## ✅ Task Breakdown (실행 계획)
- [ ] `shared-space.actions.ts`에 `createSharedSpaceDraft` Server Action 구현
- [ ] C-002의 `naver-gateway.ts`를 통해 관심매물 목록 조회(목업 모드 기본), 실연동 클라이언트(`naver-listing.ts`)는 이 태스크에서 `try/catch`(REQ-NF-004)와 함께 최초 작성
- [ ] 후보 선택 UI(shadcn/ui 체크박스 리스트) — 5개 초과 선택 시 UI 레벨에서 즉시 차단
- [ ] 서버 측에서도 6개째 진입 시 검증(클라이언트 우회 방지)
- [ ] `SharedSpace` + `ListingRef` 레코드 생성

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-S-001** 참조. 관련 AC: AC-01-01(정상), AC-01-02(경계).

## ⚙️ Technical & Non-Functional Constraints
- LIM-01, `decisions` G5: 매물 데이터는 필요한 필드만 읽고 쓰지 않는다
- LIM-10: 후보 상한 5개는 애플리케이션 레벨에서 강제(C-000의 Prisma 스키마는 이 제약을 갖지 않음)

## 🏁 Definition of Done (DoD)
- [ ] TEST-S-001의 모든 테스트가 통과하는가?
- [ ] 목업 모드에서 네트워크 의존 없이 전체 흐름이 완주되는가?

## 🚧 Dependencies & Blockers
- Depends on: C-002(목업 데이터), A-001(임시 인증)
- Blocks: S-002(초대 발급), J-009(조건 자동 재적용), V-003(매물 소진 처리), X-004(동시접속 상한 체크)
