---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] J-001: 예산 평가기 (BudgetEvaluator) [Query]"
labels: 'feature, backend, core-logic, priority:critical'
assignees: ''
---

## 🎯 Summary
- 기능명: [J-001] 예산 평가기(BudgetEvaluator) — REQ-FUNC-010A
- Read/Write 구분: **Query**(순수 계산 함수, DB 쓰기 없음 — 저장은 J-002가 별도로 담당)
- 목적: 판정 엔진 최초 동작 확인. 조건 타입 1종(상한형)만으로 5분류 판정 체계를 검증한다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-010A, §4.1.1 판정 상태 결정 로직(플로차트), §9.3 AC-10a-01·02
- 개인 구현 난이도 지시: `SRS_V0_9-AI-작업지시서.md` TASK-A2
- 데이터 계약: C-001의 `JudgmentResult` 타입, `SRS_V0_9.md` §6.2 enum `JudgmentStatus`
- 이 태스크의 인수 조건(테스트)은 TEST-J-001 참조 — 이 문서는 "어떻게 만들지"만 다룬다
- 구 버전: `SRS_project/tasks/TASK-005-FR010A-예산판정.md`(v1)에서 계산 로직만 분리(저장 로직은 J-002)

## ✅ Task Breakdown (실행 계획)
- [ ] `domain/judgment/evaluators/budget-evaluator.ts` 작성 — 순수 함수, 프레임워크·DB 비의존
- [ ] 결정 트리(원 SRS §4.1.1) 그대로 구현: 측정 대상 존재? → 계산 시도? → 기준 충족?
- [ ] 함수 시그니처: `evaluateBudget(condition: Condition, actualCost: ActualCost): JudgmentResult`(입출력은 C-001 타입 사용, DB 접근 없음)
- [ ] TEST-J-001의 테스트가 먼저 작성되어 있는지 확인 후, 그 테스트를 통과하도록 구현(TDD)

## 🧪 Acceptance Criteria (BDD/GWT)
> 이 태스크의 인수 조건은 companion Task **TEST-J-001**로 이전되었다. 이 섹션에는 원본 AC ID만 참조로 남긴다: AC-10a-01(정상), AC-10a-02(실패 — 계산 불가 분리). 전체 Given-When-Then 및 실행 가능한 테스트 스켈레톤은 TEST-J-001을 참조할 것.

## ⚙️ Technical & Non-Functional Constraints
- **제품 정체성 제약(협상 불가):** 5분류 구분(특히 미충족≠확인필요≠계산불가)은 어떤 이유로도 단순화·병합하지 않는다(`decisions/0001` 연계)
- 이 태스크는 조건 타입 1종(상한형)만 다룬다 — 하한형·유무형·일치형은 J-003~005로 분리
- 순수 함수만 허용 — DB 접근, 외부 API 호출, 부수효과 코드는 이 파일에 넣지 않는다(Query/Command 분리 원칙)

## 🏁 Definition of Done (DoD)
- [ ] TEST-J-001의 모든 테스트가 통과하는가?
- [ ] `evaluateBudget`이 순수 함수(부수효과 없음)로 구현되었는가?
- [ ] SonarQube / Linter 등의 정적 분석 도구 경고가 없는가?

## 🚧 Dependencies & Blockers
- Depends on: C-001(타입 계약), I-001(FR-002, `budgetCap` 저장 로직이 선행돼야 실 데이터 검증 가능 — 단위 테스트 자체는 목업 Condition으로 독립 진행 가능)
- Blocks: J-002(판정 저장), J-003~007(확장 평가기가 이 파일의 패턴을 재사용), V-001(FR-017 방문 후보 결정)
