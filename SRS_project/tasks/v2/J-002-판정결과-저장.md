---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] J-002: 판정 결과 저장 (JudgmentResult upsert) [Command]"
labels: 'feature, backend, priority:high'
assignees: ''
---

## 🎯 Summary
- 기능명: [J-002] 판정 결과 저장(upsert)
- Read/Write 구분: **Command**(DB 쓰기)
- 목적: J-001(및 J-003~007)의 순수 계산 결과를 `JudgmentResult` 테이블에 upsert한다. 계산(Query)과 저장(Command)을 분리해 J-001이 DB 없이도 독립 테스트 가능하게 한다(원칙 ②).

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- 데이터 계약: `SRS_V0_9.md` §6.2 `JudgmentResult` 모델(`@@unique([personId, listingId, conditionKey])`)
- 짝이 되는 Query: J-001, J-003~007의 계산 결과를 입력으로 받음
- 구 버전: `SRS_project/tasks/TASK-005-FR010A-예산판정.md`의 "queries/judgment.queries.ts에 upsert 로직 연결" 항목에서 분리

## ✅ Task Breakdown (실행 계획)
- [ ] `domain/judgment/persist-judgment-result.ts` 작성 — `JudgmentResult` upsert 함수(`personId`, `listingId`, `conditionKey` 복합키 기준)
- [ ] 조건 또는 후보 변경 시 재계산·재저장이 트리거되는 지점 정의(I-001, I-002, S-001 등 조건/매물 변경 Command에서 이 함수를 호출)
- [ ] Prisma unique 제약(`@@unique`) 위반 시 update로, 없으면 create로 분기하는 upsert 패턴 구현

## 🧪 Acceptance Criteria (BDD/GWT)
> 이 태스크 자체의 원 SRS AC는 없다(저장 메커니즘은 AC-10a-01의 SLO "조건·후보 변경 후 다음 화면 전환 전 반영"에 대한 구현 세부사항). 통합 테스트는 TEST-J-001의 assertion에 포함된다.

## ⚙️ Technical & Non-Functional Constraints
- 이 함수는 계산 로직(J-001·003~007)을 포함하지 않는다 — 계산된 `JudgmentResult` 객체를 받아 저장만 한다(Query/Command 경계 준수)
- 동시 갱신 시 마지막 쓰기가 이기는(last-write-wins) 정책을 기본으로 하며, 낙관적 잠금은 이 태스크 범위 밖(`[TBD]`)

## 🏁 Definition of Done (DoD)
- [ ] upsert가 create/update 양쪽 경로 모두에서 단위 테스트로 검증되는가?
- [ ] `@@unique` 복합키 위반 없이 재판정 시 중복 행이 생기지 않는가?
- [ ] Linter 경고가 없는가?

## 🚧 Dependencies & Blockers
- Depends on: C-000(스키마), J-001
- Blocks: J-008(1인 빈 경로 조회가 저장된 결과를 읽음), V-001(방문 후보 결정)
