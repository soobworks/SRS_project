---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] S-004: 임시조건 생애주기 — 저장·이관·만료 삭제 [Command]"
labels: 'feature, backend, auth, priority:medium'
assignees: ''
---

## 🎯 Summary
- 기능명: [S-004] B 조건 입력 및 임시 보관(REQ-FUNC-007), 결과 후 로그인(REQ-FUNC-008)
- Read/Write 구분: **Command**(DB 쓰기 — 임시 저장·이관·만료 삭제) — 세 단계 모두 같은 `Invite.tempCondition` 필드의 생애주기이므로 한 파일에 두되, 사용자 트리거(저장·이관)와 스케줄 트리거(만료 삭제)는 별도 함수로 구현한다.
- 목적: A-001의 임시 인증(초대코드 신원)을 실제 로그인 흐름으로 전환하는 지점을 완성하고, 마지막 접근 +30일 미이관 조건을 배치로 삭제한다.
- 병합 근거(`TASK-개수-축약-분석.md` #4): 저장·이관·만료 삭제는 전부 같은 `Invite.tempCondition` 필드의 생애주기이며, 원 SRS AC-07-01~03도 애초에 한 절(§9.2)에 함께 서술돼 있었다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-007·008, §9.2 AC-07-01~03, AC-08-01·02
- 임시 인증 설계: `SRS_V0_9.md` §3.1(A-001)
- Server Action: `SRS_V0_9.md` §3.1 — `saveTemporaryCondition`, `migrateTemporaryCondition`
- Route Handler: `SRS_V0_9.md` §3.2 — `/api/cron/purge-expired-conditions`(GET, Vercel Cron, 매일 1회)
- 데이터 모델: `SRS_V0_9.md` §6.2 `Invite.lastAccessedAt`(`@@index`로 배치 삭제 최적화됨)
- 인수 조건: companion **TEST-S-004** 참조
- 구 버전: `SRS_project/tasks/TASK-013-FR007008-비로그인인증전환.md`(v1)에서 분리됐던 v2 S-004/S-005를 다시 한 파일로 통합(2026-08-26, 태스크 개수 축약)

## ✅ Task Breakdown (실행 계획)

**저장·이관(사용자 트리거):**
- [ ] `condition.actions.ts`에 `saveTemporaryCondition(inviteCode, input)` 구현 — 초대 코드 단위 격리 저장
- [ ] `migrateTemporaryCondition(inviteCode, personId)` 구현 — 로그인 완료 시 임시 조건을 계정으로 이관
- [ ] 로그인 요청 시점 로직 — 비로그인 열람이 허용되는 한, 첫 결과 확인 후 저장·재방문 시도 시점에만 로그인 요청

**만료 삭제(스케줄 트리거, Cron):**
- [ ] `/api/cron/purge-expired-conditions`(Route Handler) 구현 — `lastAccessedAt` +30일 초과 & `migratedAt` null인 `Invite`의 `tempCondition` 삭제
- [ ] Vercel Cron 등록(`vercel.json`)
- [ ] 로컬 수동 트리거용 스크립트 작성(Cron 없이도 테스트 가능하도록)

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-S-004**로 이전되었다. 참조 AC: AC-07-01(정상), AC-07-02(정상), AC-07-03(예외 — 배치 삭제), AC-08-01(정상), AC-08-02(보류/예외).

## ⚙️ Technical & Non-Functional Constraints
- REQ-NF-005: 임시 조건도 초대 코드 단위로 격리, 접근 제어 적용
- AC-08-02는 GAP-002(SRS_V0_9.md §1.6)와 직결 — 네이버 로그인 프로바이더 확정 전까지는 A-001의 임시 인증을 유지
- LIM-12: 보관 기간 정확히 30일
- 이 배치는 `Invite` 레코드 자체가 아니라 `tempCondition` 필드만 삭제한다(초대 코드 자체의 존재는 유지) — 범위를 명확히 코드 리뷰에서 확인

## 🏁 Definition of Done (DoD)
- [ ] TEST-S-004의 모든 테스트가 통과하는가?
- [ ] 단위 테스트 및 통합 테스트가 추가되었고 통과하는가?
- [ ] Cron 배치가 로컬에서 수동 트리거로 검증되었는가?

## 🚧 Dependencies & Blockers
- Depends on: A-001(임시 인증), S-002(초대 코드 필요)
- Blocks: 없음
