---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] J-006: 5분류 상태 분류 및 후보 그룹화 (StatusClassifier + CandidateGroupClassifier) [Query]"
labels: 'feature, backend, core-logic, priority:critical'
assignees: ''
---

## 🎯 Summary
- 기능명: [J-006] 5분류 상태 분류기(StatusClassifier) + 후보 그룹화(CandidateGroupClassifier) — REQ-FUNC-020, REQ-FUNC-011
- Read/Write 구분: **Query**(순수 계산)
- 목적: J-001·J-003의 각 평가기 원시 결과를 5분류(MET/UNMET/CONFIRMATION_NEEDED/CALCULATION_FAILED/NOT_APPLICABLE)로 통일한 뒤, A·B 개인별 5분류를 종합해 후보 매물을 "둘 다 충족"/"한쪽만 충족"/"둘 다 불충족" 3분류로 그룹화한다. **제품 정체성과 가장 직결되는 태스크.** 이 태스크 완료로 J-001~006(판정 엔진 전체)이 완결된다.
- 병합 근거(`TASK-개수-축약-분석.md` #3): 두 함수 모두 같은 판정결과셋을 순차 처리하는 파이프라인(분류 → 그룹화)이며, TEST-J-EXT가 이미 둘을 하나로 검증하고 있었다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-020·011, §4.1.1 판정 상태 결정 로직(플로차트), §9.3 AC-20-01~03, AC-11-01·02
- 데이터 계약: C-001, `SRS_V0_9.md` §6.2 enum `JudgmentStatus`(그룹 3분류는 별도 enum 없이 계산 시점 파생값)
- 인수 조건: companion **TEST-J-EXT** 참조(AC-20-01~03, AC-11-01·02 포함)
- 구 버전: `SRS_project/tasks/TASK-006-FR010B011020-확장조건판정.md`(v1)에서 분리됐던 v2 J-006/J-007을 다시 한 파일로 통합(2026-08-26, 태스크 개수 축약)

## ✅ Task Breakdown (실행 계획)

**① 상태 분류(StatusClassifier):**
- [ ] `domain/judgment/status-classifier.ts` 작성 — J-001·J-003의 4개 평가기 원시 결과를 입력받아 5분류로 정규화
- [ ] "출근 안 함" 등 측정 대상 자체가 없는 경우 NOT_APPLICABLE로 분류하는 분기(AC-20-03) 구현
- [ ] TEST-J-EXT의 AC-20-01~03 테스트를 통과하도록 구현

**② 후보 그룹화(CandidateGroupClassifier):**
- [ ] `domain/judgment/candidate-group-classifier.ts` 작성 — A·B 각각의 ①단계 분류 결과를 입력받아 3분류 산출
- [ ] "확인 필요" 배지 분리 로직 — 3분류 중 하나로 흡수하지 않고 별도 배지로 표시할 데이터 형태 반환(AC-11-02)
- [ ] TEST-J-EXT의 AC-11-01·02 테스트를 통과하도록 구현
- [ ] 이 시점에 J-001~006(판정 엔진 전체) 통합 테스트(TEST-J-EXT)가 전부 통과하는지 최종 확인

## 🧪 Acceptance Criteria (BDD/GWT)
> 인수 조건은 companion Task **TEST-J-EXT**로 이전되었다. 분류: AC-20-01(정상), AC-20-02(실패 — 계산불가/확인필요 구분), AC-20-03(예외 — 해당없음). 그룹화: AC-11-01(정상 — 3분류), AC-11-02(예외 — 확인 필요 배지 분리, 종합 판정 대체 금지).

## ⚙️ Technical & Non-Functional Constraints
- **제품 정체성 제약(협상 불가):** 4개 상태(미충족/계산불가/확인필요/해당없음) 간 오분류는 이 프로젝트에서 가장 치명적인 버그 유형이다 — J-001·J-003의 모든 평가기가 이 분류기를 거치는지 코드 리뷰에서 확인. 3분류·확인필요 배지 어디에도 총점·복합순위·공동 적합도를 산출하는 코드를 넣지 않는다(`decisions/0001`)
- 순수 함수만 허용, DB·외부 API 접근 금지

## 🏁 Definition of Done (DoD)
- [ ] TEST-J-EXT의 AC-20-01~03, AC-11-01·02 테스트가 모두 통과하는가?
- [ ] J-001, J-003 전부가 분류기를 거쳐 결과를 반환하는가(개별 평가기가 자체적으로 상태를 확정하지 않는가)?
- [ ] 코드 리뷰에서 총점·추천배지·복합순위 관련 코드가 없음을 확인했는가?

## 🚧 Dependencies & Blockers
- Depends on: J-001, J-003(전체 4개 평가기)
- Blocks: S-001 이후 모든 판정 결과 소비 태스크(V-001, R-001, X-001 등) — 판정 엔진의 마지막 조각
