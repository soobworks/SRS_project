---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] NF-011: 동시접속자 상한 및 저하 전략 (완전 무료 운영)"
labels: 'feature, infra, priority:medium'
assignees: ''
---

## 🎯 Summary
- 기능명: [REQ-NF-011, 신규] 동시접속자 상한 + 저하 전략
- 목적: "완전 무료" 인프라 운영 전제(C-TEC-008)를 실제로 지키기 위해, 동시 활성 공유 객체 수에 상한을 두고 상한 도달 시 조용히 장애가 나는 대신 명시적으로 저하한다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- **⚠️ 이 태스크의 근거는 아직 `SRS_V0_9.md` 본문에 병합되지 않았다.** 설계 원문은 `SRS_V0_9-AI-작업지시서.md` TASK-B1(C-TEC-008 신설)·TASK-B4(§11.3 신설, REQ-NF-011)·TASK-B5(pgbouncer 풀링)를 그대로 따른다. 착수 전 이 세 TASK 항목이 실제로 SRS에 반영됐는지 먼저 확인할 것.
- 3관점 검토: `SRS_V0_9-작업지시서-변경사항-3관점-검토.md` §2.3 — 이 기능이 "기술스택 정합화"가 아니라 **작은 신규 기능**이라는 지적을 반드시 먼저 읽을 것

## ✅ Task Breakdown (실행 계획)
- [ ] (선행) `SRS_V0_9-AI-작업지시서.md` TASK-B1·B4·B5가 SRS 본문에 반영됐는지 확인, 미반영이면 먼저 반영
- [ ] `SharedSpace` 테이블에 대한 `COUNT` 쿼리로 현재 활성 공유 객체 수를 조회하는 `checkCapacityBeforeInvite(): Promise<{ available: boolean }>` Server Action 구현
- [ ] `inviteParticipant`(TASK-008) 호출 직전에 `checkCapacityBeforeInvite`를 선행 호출하도록 연결
- [ ] 상한 초과 시 안내 화면(정원 초과 메시지) 구현 — 새 인프라·큐 도입 없이 단일 쿼리로 구현(C-TEC-008 준수)
- [ ] 배포 환경 `DATABASE_URL`을 Supabase pgbouncer 풀링 연결로 전환(TASK-B5, 포트 6543)

## 🧪 Acceptance Criteria (BDD/GWT)
Scenario 1: 상한 이내 정상 초대
- Given: 활성 공유 객체 수가 상한 이하인 상태
- When: A가 초대를 발송하려 함
- Then: `checkCapacityBeforeInvite`가 `available: true`를 반환하고 초대가 정상 진행된다.

Scenario 2: 상한 도달 시 저하
- Given: 활성 공유 객체 수가 상한(N, 배포 전 확정)에 도달한 상태
- When: 새로운 사용자가 초대를 시도함
- Then: 새 기능을 만들지 않고 정원 초과 안내 화면으로 대체하며, 기존 활성 세션은 영향받지 않는다.

## ⚙️ Technical & Non-Functional Constraints
- **이 태스크는 원 SRS(`같이보기-srs-v1_0.md`)의 REQ-FUNC-001~025 어디에도 명시되지 않은 신규 요구사항이다.** 착수 전 REQ-NF-011을 `SRS_V0_9.md` §5.2(기술스택발 신규 NFR, 3관점 검토가 권고한 위치)로 정식 등록할 것을 권고
- 상한 값 "N"은 `[TBD]` — 배포 직전 Vercel/Supabase 무료 티어 한도 재확인 후 확정
- pgbouncer 미적용 시 낮은 동시성에서도 DB 연결 고갈 장애가 날 수 있음(가장 흔한 실수, TASK-000과 연동 확인)

## 🏁 Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria를 충족하는가?
- [ ] REQ-NF-011이 SRS 문서에 정식 등록되었는가(누락 시 이 태스크를 완료로 처리하지 않는다)?
- [ ] `DATABASE_URL`이 pgbouncer 풀링 연결로 확인되었는가?

## 🚧 Dependencies & Blockers
- Depends on: TASK-000(pgbouncer 설정), TASK-007(FR-001, SharedSpace 카운트 대상)
- Blocks: 없음
