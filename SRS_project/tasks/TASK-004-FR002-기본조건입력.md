---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] FR-002: A 기본 조건 입력 (예산 · 출퇴근)"
labels: 'feature, backend, priority:high'
assignees: ''
---

## 🎯 Summary
- 기능명: [FR-002] A 기본 조건 입력
- 목적: 예산(항상 필수)과 출퇴근 조건(조건부)을 입력받아 판정 엔진(TASK-005)의 최소 입력을 확보한다.

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-002, §9.1 AC-02-01~03
- Server Action 시그니처: `SRS_V0_9.md` §3.1 — `saveBudgetAndCommute(personId: string, input: ConditionInput): Promise<Condition>`
- 데이터 모델: `SRS_V0_9.md` §6.2 `Person.budgetCap`, `commutes`, `commuteOrigin`, `commuteMode`

## ✅ Task Breakdown (실행 계획)
- [ ] `condition.actions.ts`에 `saveBudgetAndCommute` Server Action 구현
- [ ] 예산 입력 폼(shadcn/ui) — 예산 미입력 시 저장 자체를 막는 클라이언트·서버 양쪽 검증
- [ ] 출퇴근 여부 토글 → `commutes=true` 선택 시에만 출근지·이동수단 입력 필드 노출(점진적 공개)
- [ ] `commutes=false` 선택 시 `commuteOrigin`/`commuteMode`를 null로 저장, 통근 판정을 "해당 없음"으로 이어지도록 스키마 레벨에서 보장

## 🧪 Acceptance Criteria (BDD/GWT)
Scenario 1: 예산 없이는 완료 불가 (원 SRS AC-02-01, 실패)
- Given: 예산이 입력되지 않은 상태
- When: A가 기본 입력을 완료하려 함
- Then: 기본 입력 완료를 막는다(예산 없이 완료 처리 0건).

Scenario 2: 출근함 선택 시 추가 입력 요청 (원 SRS AC-02-02)
- Given: A가 예산을 입력한 상태
- When: 출퇴근 여부를 질문하고 A가 "출근함"을 선택함
- Then: 출근지와 이동수단 입력을 요청한다(필수 입력 순서 위반 0건).

Scenario 3: 출근 안 함 선택 시 강제 요구 없음 (원 SRS AC-02-03, 예외)
- Given: A가 예산을 입력한 상태
- When: A가 "출근 안 함"을 선택함
- Then: 출근지·이동수단을 요구하지 않고 통근·교통비를 판정에서 제외한다(출근지 강제 요구 0건).

## ⚙️ Technical & Non-Functional Constraints
- 성능: Server Action 응답은 이 문서군의 E2E SLO(P95 ≤ 30초, REQ-NF-001) 예산 안에 포함되는 하위 구간이므로 별도 지연 유발 로직(불필요한 외부 호출 등) 금지
- 정확성(REQ-NF-006): 저장된 값은 이후 화면 어디서든 전제 없이 노출되지 않도록 TASK-016과 연동 예정
- 개인 구현 난이도 조정(TASK-A2 원칙): 이 태스크는 조건 입력 UI만 다루며, 판정 로직은 포함하지 않는다(TASK-005로 분리)

## 🏁 Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria를 충족하는가?
- [ ] 단위 테스트 및 통합 테스트가 추가되었고 통과하는가?
- [ ] Linter 경고가 없는가?
- [ ] 폼 컴포넌트가 shadcn/ui 표준 컴포넌트만으로 구성되었는가(C-TEC-004)?

## 🚧 Dependencies & Blockers
- Depends on: TASK-002(임시 인증 — `PersonId` 필요)
- Blocks: TASK-005(FR-010A, 예산 판정은 이 태스크가 저장한 `budgetCap`이 있어야 동작)
