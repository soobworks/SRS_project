---
name: Feature Task
about: SRS 기반의 구체적인 개발 태스크 명세
title: "[Feature] FR-010B/011/020: 확장 조건 판정 · 후보 그룹화 · 5분류 오류 분리"
labels: 'feature, backend, core-logic, priority:critical'
assignees: ''
---

## 🎯 Summary
- 기능명: [FR-010B] 확장 조건 판정, [FR-011] 후보 목록 그룹화, [FR-020] 상태·계산 오류 분리
- 목적: 판정 엔진의 나머지 3개 조건 타입(하한형·유무형·일치형)을 순차 추가하고, 후보를 3분류로 그룹화하며, 5분류 원칙이 전체 조건 타입에 걸쳐 무너지지 않음을 확정한다. **이 프로젝트에서 난이도가 가장 높은 태스크.**

## 🔗 References (Spec & Context)
> 💡 AI Agent & Dev Note: 작업 시작 전 아래 문서를 반드시 먼저 Read/Evaluate 할 것.
- SRS 문서: `같이보기-srs-v1_0.md` §4.1 REQ-FUNC-010B·011·020, §9.3 AC-10b-01~03, AC-11-01·02, AC-20-01~03
- 개인 구현 난이도 지시: `SRS_V0_9-AI-작업지시서.md` TASK-A2 — "②LowerBoundEvaluator→PresenceEvaluator→ExactMatchEvaluator 순서로 추가, ③매 단계마다 5분류 전부를 검증하는 테스트를 먼저 작성(TDD)"
- 확장 패턴: `SRS_V0_9.md` §5.1 REQ-NF-007(`ConditionTypeRegistry`, 신규 타입 추가 시 코드 변경 최소화)

## ✅ Task Breakdown (실행 계획, 조건 타입별 순차 진행)
- [ ] `domain/judgment/evaluators/lower-bound-evaluator.ts`(전용면적) 작성 + 단위 테스트 선(先) 작성 → 통과 확인
- [ ] `domain/judgment/evaluators/presence-evaluator.ts`(주차) 작성 + 단위 테스트 선 작성 → 통과 확인
- [ ] `domain/judgment/evaluators/exact-match-evaluator.ts`(매물유형) 작성 + 단위 테스트 선 작성 → 통과 확인
- [ ] `domain/judgment/evaluators/index.ts`에 `ConditionTypeRegistry` 구현 — enum→평가기 매핑, 신규 타입 추가 시 이 파일만 수정하면 되도록 설계
- [ ] `domain/judgment/candidate-group-classifier.ts` 작성 — "둘 다 충족"/"한쪽만 충족"/"둘 다 불충족" 3분류, "확인 필요" 배지 분리 로직
- [ ] `queries/judgment.queries.ts`에 4개 조건 유형 통합 판정 결과 조회 로직 완성

## 🧪 Acceptance Criteria (BDD/GWT)
Scenario 1: 4개 조건 유형 지원 (원 SRS AC-10b-01)
- Given: FR-10a 판정이 선행 완료된 상태
- When: 추가 필수 조건(0~4개)을 설정함
- Then: 조건 유형에 맞는 연산으로 충족·미충족·미달량을 산출한다(4개 조건 유형 전체 지원).

Scenario 2: 데이터 부재는 확인 필요로 분리 (원 SRS AC-10b-02, 실패)
- Given: 매물 데이터에 판정 필요 필드가 없는 상태
- When: 해당 조건을 판정하려 함
- Then: "확인 필요"로 분리하고 "미충족"으로 처리하지 않는다(데이터 부재 항목의 미충족 오분류 0건).

Scenario 3: 총점 생성 금지 (원 SRS AC-10b-03, 경계 — 제품 정체성 핵심)
- Given: 사람 조건이나 후보가 추가·수정된 상태
- When: 6개 판정 항목 전체가 재계산됨
- Then: 어떤 조건 조합에서도 공동 총점을 생성하지 않는다(총점 생성 0건).

Scenario 4: 후보 3분류 그룹화 (원 SRS AC-11-01)
- Given: A·B의 현재 입력이 판정에 반영된 상태
- When: 후보 목록을 조회함
- Then: 각 후보를 "둘 다 충족"·"한쪽만 충족"·"둘 다 불충족" 중 하나로 표시한다(그룹 분류 3종, 미분류 후보 0건).

Scenario 5: 확인 필요를 종합 판정으로 대체 금지 (원 SRS AC-11-02, 예외)
- Given: 확인이 필요한 후보가 존재하는 상태
- When: 목록이 렌더링됨
- Then: 최소 수준의 "확인 필요" 배지를 별도로 표시하고 종합 판정으로 대체하지 않는다.

Scenario 6: 계산 불가/확인 필요/해당 없음 오분류 금지 (원 SRS AC-20-02, 실패)
- Given: 경로·데이터 계산을 시도한 상태
- When: 계산이 실패하거나 데이터가 없어 확인이 필요함
- Then: 각각 "계산 불가"·"확인 필요"로 표시하고 "미충족"으로 분류하지 않는다(4개 상태 간 오분류 0건).

## ⚙️ Technical & Non-Functional Constraints
- **제품 정체성 제약(협상 불가):** 총점·복합 순위·공동 적합도 생성 금지(`decisions/0001`). 이 태스크의 모든 PR은 총점 관련 코드가 없는지 리뷰 체크리스트에 명시적으로 포함한다
- TDD 필수 — TASK-A3(§5.3) 방침에 따라 위 6개 Scenario를 구현 전에 자동화 테스트로 먼저 작성한다
- REQ-NF-007: 신규 조건 타입 추가가 `ConditionTypeRegistry` 등록 1줄로 가능한지 코드 리뷰에서 확인

## 🏁 Definition of Done (DoD)
- [ ] 모든 Acceptance Criteria(6개 Scenario)를 충족하는가?
- [ ] 4개 조건 타입 각각에 대해 5분류 전체를 커버하는 단위 테스트가 존재하고 통과하는가?
- [ ] 코드 리뷰에서 총점·추천배지·복합순위 관련 코드가 없음을 확인했는가?
- [ ] SonarQube / Linter 경고가 없는가?

## 🚧 Dependencies & Blockers
- Depends on: TASK-005(FR-010A, `BudgetEvaluator`와 `StatusClassifier` 골격 재사용)
- Blocks: TASK-007(FR-001), TASK-009(FR-017), TASK-010(FR-012), TASK-012(FR-003·004), TASK-014(FR-018·019), TASK-016(FR-021), TASK-018(FR-022) — 판정 결과를 소비하는 사실상 모든 후속 태스크
