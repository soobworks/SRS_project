/**
 * 프로토타입 고정 식별자 — 폐기 예정
 *
 * `A-001`(임시 인증)을 범위에서 뺐으므로 세션이 없다.
 * `PersonId`·`spaceId`를 여기 한 곳에 모아두고, `A-001` 원 순번에서
 * 세션 조회로 교체한다. 화면 코드 여러 곳에 흩뿌리지 않는다.
 */
export const DEMO_SPACE_ID = "demo";
export const DEMO_INVITE_CODE = "DEMOCODE";
export const DEMO_PERSON_A = "person-a";
export const DEMO_PERSON_B = "person-b";
