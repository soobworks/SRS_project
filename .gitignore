.DS_Store
Thumbs.db
*.tmp
~$*

# copyrighted standard document — reference only, not for public redistribution
SRS_project/29148-2018-ISOIECIEEE.pdf

# HTML renderings stay local (PC folder) only — never pushed to GitHub
*.html

# Claude Code local permission overrides — per-developer, not shared
.claude/settings.local.json

# --- Next.js 프로토타입 (2026-08-27, 이슈 #55) ---
node_modules/
.next/
out/
build/
.env
.env*.local
next-env.d.ts
.vercel
*.tsbuildinfo

# 프로토타입 화면 캡처는 로컬 산출물 — 용량이 커 저장소에 넣지 않는다
reports/prototype-screens/
